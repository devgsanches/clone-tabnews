function Home() {
  return <h1>Realmente amor, ninguém test 😔</h1>;
}

// por padrão, o navegador já lê a pages, sem precisarmos indicar no path, pois o index é interpretado como arquivo principal do sistema

export default Home;
